export enum Url {
    streaks = 'api/member/streaks', // Leaving it be as it is unused
    rewardPolicy = 'api/utils/rewardpolicies',
    rewardPolicies = 'api/rewards/all-rewardpolicies',
    rewards = 'api/utils/rewards', // Leaving it be as it is unused
    perks = 'api/rewards/perks', // Leaving it be as it is unused
    buy = 'api/rewards/buy',
    aggregate = 'api/rewards/aggregate', // Leaving it be as it is unused
    products = "api/utils/products", // Leaving it be as it is unused
    otherProducts = "api/utils/other-products" // Leaving it be as it is unused
}
